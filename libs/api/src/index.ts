import ContactUsEndpoint from "./lib/endpoints/ContactUsEndpoint";
import CustomersEndpoint from "./lib/CustomersEndpoint";

export const api = {
  contactUs: ContactUsEndpoint,
  customers: CustomersEndpoint,
}