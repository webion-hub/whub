{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/elfo",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/elfo",
        "outputPath": "dist/apps/elfo"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/elfo"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "elfo:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "elfo:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "elfo:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "elfo:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/elfo"],
      "options": {
        "jestConfig": "apps/elfo/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/elfo/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
